# API (FastAPI)

Planned endpoints:
- `POST /ingest/folder`
- `GET /cards`
- `GET /cards/{id}`
- `PATCH /cards/{id}`
- `GET /cards/{id}/revisions`
- `POST /cards/{id}/revert/{revision}`
- `GET /healthz`
